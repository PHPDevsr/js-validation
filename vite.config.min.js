import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    lib: {
      entry: './src/index.js',
      name: 'jsValidation',
      formats: ['umd', 'es'],
      fileName: (format) => {
        if (format === 'umd') return 'js-validation.min.js';
        return 'js-validation.esm.min.js';
      }
    },
    outDir: 'dist',
    emptyOutDir: false,
    minify: 'terser',
    rollupOptions: {
      output: {
        exports: 'named'
      }
    }
  }
});
